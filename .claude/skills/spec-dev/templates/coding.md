# 実装とテスト: <feature>

- 作成: YYYY-MM-DD / 状態: draft | approved
- 対応設計: design.md

要件から設計までは requirements/spec/design が持っている。この文書が持つのは
**その先の 2 本の対応** — 設計がどのファイルになったか (D↔コード) と、仕様の
受入条件をどのテストが確かめているか (S↔T)。どちらもここにしか無い。
実装コードのコメントには ID を書かない (番号は修正時に直らず残るため。
対応はこの文書に集約する)。

## D↔コード

| D | 実装 |
|---|---|
| D-01 <名前> | `path/to/file` の `functionName` |

### 設計から変えた点 (実装して分かったこと)

- **D-nn**: (何を変えたか。実装しないと分からなかった理由も書く。
  design.md 本体も取り消し線 + 理由で更新すること)

## S↔T

| S | 受入条件を確かめるテスト |
|---|---|
| S-01 <名前> | T-01〜T-03 (`test/path/to/test_file`) |

T-ID はテスト本体のコメントに書く (`// T-01: S-01 ...`)。この表と実物が
ずれていないことは `trace-matrix.py --code <src> --code <test> docs/<feature>/`
で機械的に確かめる。

## 検証していないこと

自動テストで届かない範囲。実機・手動でしか確認できないもの (対応する R/S を添える):

- (R-nn) <確認内容と手順>
