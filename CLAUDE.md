# プロジェクト規約

## 文章規約
- 日本語の文章中でも括弧は半角 `()` を使う。全角 `（）` は使わない。

## ディレクトリ規約

トレース ID: R = 要件、S = 仕様、D = 設計、T = テスト。要件→仕様→設計→テストを
ID で相互参照し (例: 「S-01 は R-01 を実現」)、問題が出たとき影響範囲を grep で辿れるようにする。

| 場所 | 内容 | 運用 |
|---|---|---|
| TODO.md | やることリスト | 追記し、完了したら削除する (履歴は git と notes/ が持つ) |
| docs/\<feature\>/ | 要件・仕様・設計 = 現在の正 (R/S/D の ID 付き) | 常に最新に更新する。**判断を変えた箇所だけ** ~~取り消し線~~ + 「変更 (日付): 理由」で痕跡を残す。誤字・表現・追記は普通に上書きしてよい。**ID ごと撤回・移管するときは取り消し線でなく、本文を削除して文書末尾の「退役 (番号は再利用しない)」台帳へ 1 行** (履歴は git が持つ)。ADR は追記のみ (覆すときは新しい ADR で置き換えを宣言) |
| notes/ | 調査メモ・作業記録 (YYYYMMDD-\<topic\>.md) | **追記のみ**。過去の記録は書き換えない。**プログラムを置かない** (捨てスクリプトはメモ内のコードブロックとして記録。ビルドツールは .gitignore を見ずにツリーを歩くため) |
| notes/fix-sessions/ | fix-loop の作業台帳 | 同上 |
| notes/reviews/ | ローカルレビュー結果の全文 (冒頭に対象コミット) | 同上 |
| notes/review-log.md | 繰り返し出るレビュー指摘の台帳 | 追記のみ。3 回出た指摘はチェックリストへ昇格 |

## TODO 運用
- レビュー指摘のうち今直さないものは 1 行で追記する:
  `- [ ] 要点 (file:line, S-ID) — 詳細: notes/reviews/YYYYMMDD-<topic>.md`
- リンクは TODO → notes/ の片方向のみ (TODO は消し込むので逆向きは古くなる)
- TODO を拾って修正するときの規模判定:
  - 挙動を変えない小修正 (関数抽出・ガード節化・命名) → fix-loop のみ。docs 不要
  - 設計判断を伴う構造変更 (統合・基底クラス化など) → design.md + ADR を書いてから実装
  - 挙動が変わる / 仕様起因 → rework で上流文書から直す
- ドキュメントを書くのは「判断があった所」だけ (全部に要件から書き始めない)
