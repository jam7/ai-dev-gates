# Requirements

### R-01: Read a configuration file
The loader accepts a YAML file.

### R-02: Fail loudly on a broken file
An unreadable file stops the program.

### R-03: Arguments win over the file
Nothing downstream refers to this one, on purpose.
